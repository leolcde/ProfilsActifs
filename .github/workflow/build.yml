name: Build Check

on:
  push:
    branches: [dev]
  pull_request:
    branches: [production]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install & build
        run: |
          # adapte selon ton stack (Go / Vue.js)
          echo "build steps ici"